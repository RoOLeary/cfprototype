const Sessions = ({ content }) => {
  console.log('content', content)
  const {
    blockType,
    ctaLabel,
    ctaLink,
    hasCta,
    heading,
    isComingSoon,
    selectedSession,
    text,
    uid
  } = content
  return(
    <h1>Sessions Sessions!!!</h1>
  )
}

export default Sessions;

